export const environment = {
  production: true,
  // Reemplazar por la URL real del servicio desplegado en Render, por ejemplo:
  // https://fundocorp-backend.onrender.com/api
  apiUrl: 'https://fundocorp-backend.onrender.com/api'
};
